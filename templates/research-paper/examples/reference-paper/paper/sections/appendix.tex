\section{Validation contract}

The full release gate builds both bundled themes, validates internal and
external references, checks artifact safety and accessibility markers, compares
repeat builds, bootstraps a fresh standalone project, and compiles the extracted
arXiv archive. Publisher-specific adapters are outside version 0.1.0.
