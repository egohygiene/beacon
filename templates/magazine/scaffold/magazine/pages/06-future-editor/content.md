> The interface may change. The edition should remain understandable without it.

A future editor can load the edition manifest, arrange page references, edit
page fields and Markdown, preview layouts, and synchronize a branch. Git keeps
the history. Pull requests keep review explicit. Beacon keeps compilation and
validation deterministic.

This boundary supports richer authoring later without making today's workflow
wait for a full application.
